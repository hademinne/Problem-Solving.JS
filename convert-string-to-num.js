/*
convert string to number: we need a function that can transform a string to a number
*/

var stringToNumber = function (str) {
  // return Number(str);
  return +str;
};
console.log(stringToNumber("123"));
