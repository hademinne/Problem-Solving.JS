/*
convert number to a string :
we need a function can transform a number to a string .
*/
function numberToString(num) {
  // return num.toString();
  return `${num}`;
}

console.log(typeof numberToString(123));
